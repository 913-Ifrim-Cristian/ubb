//
// Created by Cristi Ifrim on 3/4/2022.
//
#include "vector.h"
#include "ui.h"
#include "tests.h"


int main() {

    const int howToSort = 0;

    runTests();
    /*Vector* repo = createVector(15, &destroyCountry, &copyCountry);
    History* history = createHistory();
    //History* commandHistory = createHistory();

    initRepo(repo);

    start(repo, history);*/
    return 0;
}
